[
  {
    "name": "ios_test",
    "description": "Start an iOS build + test run on the CodeBuild macOS fleet for a given branch and scheme. Returns immediately with a build_id and status IN_PROGRESS. Poll ios_build_status with the build_id until the status is no longer IN_PROGRESS.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "branch": { "type": "string", "description": "Git branch or commit SHA to test" },
        "scheme": { "type": "string", "description": "Xcode scheme to build and test" },
        "device": { "type": "string", "description": "Simulator device name (e.g. 'iPhone 17')" },
        "os_version": { "type": "string", "description": "iOS version (e.g. '18.2' or 'latest')" },
        "test_plan": { "type": "string", "description": "Optional test plan name (empty = default)" },
        "repo": { "type": "string", "description": "Optional GitHub repo URL to build (multi-app: point the shared project at this repo for this run). Omit to use the project's configured repo." },
        "project_dir": { "type": "string", "description": "Optional subdir holding the .xcworkspace/.xcodeproj (e.g. 'ios'). Omit to use the project default." }
      },
      "required": ["branch", "scheme"]
    }
  },
  {
    "name": "ios_build_status",
    "description": "Check a CodeBuild build by id and return structured test results. While running, returns status IN_PROGRESS. When done, returns SUCCEEDED | FAILED | BUILD_ERROR | TIMED_OUT with a test summary, per-test failures, and presigned artifact URLs (xcresult zip, screenshots, logs link).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "build_id": { "type": "string", "description": "CodeBuild build id returned by ios_test" }
      },
      "required": ["build_id"]
    }
  },
  {
    "name": "list_schemes",
    "description": "List the Xcode schemes available on a branch, plus the default scheme. Reads the scheme manifest the buildspec publishes on each run; if none exists yet for the branch, returns empty.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "branch": { "type": "string", "description": "Branch name (default 'main')" }
      },
      "required": []
    }
  },
  {
    "name": "get_test_logs",
    "description": "Get detailed output, class name, duration, and failure screenshots for a single failed test in a completed build. Use this after ios_build_status reports failures to understand why a specific test failed.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "build_id": { "type": "string", "description": "CodeBuild build id" },
        "test_name": { "type": "string", "description": "Name of the failed test (from failures[].test_name)" }
      },
      "required": ["build_id", "test_name"]
    }
  },
  {
    "name": "get_build_log",
    "description": "Get the raw build output for a build, including builds that FAILED BEFORE any test ran (compile error, dependency resolution failure, scheme or simulator not found, bad project_dir). Returns error_tail (the error: lines plus the last ~100 log lines) and a presigned URL to the full build_output.log. Use this when ios_build_status returns status BUILD_ERROR or a test_summary with total 0 (get_test_logs cannot help there, since it keys off named test failures that do not exist).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "build_id": { "type": "string", "description": "CodeBuild build id returned by ios_test" }
      },
      "required": ["build_id"]
    }
  }
]
